diff -up old/HibernateService.java new/HibernateService.java
--- old/HibernateService.java	2003-12-17 15:07:00.000000000 +0100
+++ new/HibernateService.java	2004-01-13 12:46:51.000000000 +0100
@@ -271,6 +271,34 @@ public class HibernateService implements
 		setProperty(Environment.QUERY_SUBSTITUTIONS, substitutions);
 	}
 
+	public boolean isAutoCreateSchema() {
+		if ("create".equals(Environment.HBM2DDL_AUTO)
+				|| "create-drop".equals(Environment.HBM2DDL_AUTO))
+			return true;
+		else
+			return false;
+	}
+
+	public boolean isAutoDropSchema() {
+		return "create-drop".equals(Environment.HBM2DDL_AUTO);
+	}
+
+	public boolean isAutoUpdateSchema() {
+		return "update".equals(Environment.HBM2DDL_AUTO);
+	}
+
+	public void setAutoCreateSchema(boolean b) {
+		if(b) setProperty(Environment.HBM2DDL_AUTO,"create");
+	}
+
+	public void setAutoDropSchema(boolean b) {
+		if(b) setProperty(Environment.HBM2DDL_AUTO,"create-drop");
+	}
+
+	public void setAutoUpdateSchema(boolean b) {
+		if(b) setProperty(Environment.HBM2DDL_AUTO,"update");
+	}
+	
 }
 
 
diff -up old/HibernateServiceMBean.java new/HibernateServiceMBean.java
--- old/HibernateServiceMBean.java	2003-12-17 15:07:00.000000000 +0100
+++ new/HibernateServiceMBean.java	2004-01-13 12:46:54.000000000 +0100
@@ -226,6 +226,42 @@ public interface HibernateServiceMBean {
 	public void setShowSql(boolean showSql);
 	
 	/**
+	 * Is AutoCreateSchema enabled?
+	 * @return boolean
+	 */
+	public boolean isAutoCreateSchema();
+	
+	/**
+	 * Enable the create schema using hbm2ddl tool
+	 * @param showSql
+	 */
+	void setAutoCreateSchema(boolean b);
+
+	/**
+	 * Is AutoDropSchema enabled?
+	 * @return boolean
+	 */
+	public boolean isAutoDropSchema() ;
+	
+	/**
+	 * Enable the create-drop schema using hbm2ddl tool
+	 * @param showSql
+	 */
+	void setAutoDropSchema(boolean b);
+
+	/**
+	 * Is AutoUpdateSchema enabled?
+	 * @return boolean
+	 */
+	public boolean isAutoUpdateSchema();
+
+	/**
+	 * Enable the update schema using hbm2ddl tool
+	 * @param showSql
+	 */
+	void setAutoUpdateSchema(boolean b); 
+		
+	/**
 	 * Create the <tt>SessionFactory</tt> and bind to the jndi name on startup
 	 */
 	public void start() throws HibernateException;
