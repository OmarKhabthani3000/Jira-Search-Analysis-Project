Index: TemplateHelper.java
===================================================================
RCS file: /cvsroot/hibernate/HibernateExt/tools/src/java/org/hibernate/tool/hbm2x/TemplateHelper.java,v
retrieving revision 1.3
diff -u -r1.3 TemplateHelper.java
--- TemplateHelper.java	19 Jan 2005 21:19:55 -0000	1.3
+++ TemplateHelper.java	4 Apr 2005 01:56:20 -0000
@@ -19,6 +19,11 @@
 import org.apache.velocity.runtime.resource.loader.ClasspathResourceLoader;
 import org.apache.velocity.runtime.resource.loader.FileResourceLoader;
 import org.apache.velocity.tools.generic.log.CommonsLogLogSystem;
+import org.apache.velocity.tools.generic.DateTool;
+import org.apache.velocity.tools.generic.MathTool;
+import org.apache.velocity.tools.generic.NumberTool;
+import org.apache.velocity.tools.generic.IteratorTool;
+import org.hibernate.tool.hbm2x.StringUtils;
 
 /**
  * 
@@ -69,6 +74,11 @@
         getContext().put("c2j", c2j);
         getContext().put("c2h", c2h);
         getContext().put("render", new RenderTool(getEngine()));
+		getContext().put("strings", new StringUtils());
+		getContext().put("date", new DateTool());
+		getContext().put("math", new MathTool());
+		getContext().put("numberTool", new NumberTool());
+		getContext().put("iteratorTool", new IteratorTool());
 
     }
     
