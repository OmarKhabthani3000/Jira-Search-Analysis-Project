### Eclipse Workspace Patch 1.0
#P org.hibernate.eclipse.console
Index: src/org/hibernate/eclipse/console/viewers/xpl/MTreeViewer.java
===================================================================
--- src/org/hibernate/eclipse/console/viewers/xpl/MTreeViewer.java	(revision 15951)
+++ src/org/hibernate/eclipse/console/viewers/xpl/MTreeViewer.java	(working copy)
@@ -17,9 +17,12 @@
 import org.eclipse.jface.viewers.AbstractTreeViewer;
 import org.eclipse.jface.viewers.TreePath;
 import org.eclipse.jface.viewers.TreeViewer;
+import org.eclipse.jface.viewers.ViewerComparator;
 import org.eclipse.swt.widgets.Composite;
 import org.eclipse.swt.widgets.Item;
 import org.eclipse.swt.widgets.Widget;
+import org.hibernate.cfg.Configuration;
+import org.hibernate.eclipse.console.workbench.LazyDatabaseSchema;
 
 /**
  * TreeViewer has the next lack design to work with the objects as TreeItem id:
@@ -44,6 +47,18 @@
 
 	public MTreeViewer(Composite parent, int style) {
 		super(parent, style);
+		setComparator(new ViewerComparator(){
+			
+			@Override
+			public int category(Object element) {
+				if (element instanceof Configuration){
+					return -1;
+				} else if (element instanceof LazyDatabaseSchema){
+					return 1;
+				}
+				return super.category(element);
+			}
+		});
 	}
 
 	/**
