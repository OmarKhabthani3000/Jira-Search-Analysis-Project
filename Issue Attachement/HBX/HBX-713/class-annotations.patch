Index: tools/src/java/org/hibernate/tool/hbm2x/pojo/BasicPOJOClass.java
===================================================================
--- tools/src/java/org/hibernate/tool/hbm2x/pojo/BasicPOJOClass.java	(revision 10123)
+++ tools/src/java/org/hibernate/tool/hbm2x/pojo/BasicPOJOClass.java	(working copy)
@@ -141,6 +141,22 @@
 			return c2j.toJavaDoc( getMetaAsString( CLASS_DESCRIPTION ), indent );
 		}
 	}
+
+	public String getClassAnnotations() {
+		StringBuffer classAnnotations = new StringBuffer("");
+		
+                MetaAttribute metaAttribute = meta.getMetaAttribute("class-annotation");
+                if(metaAttribute!=null) {
+                        Iterator values = metaAttribute.getValues().iterator();
+                        while ( values.hasNext() ) {
+                                String annotation = (String) values.next();
+                                classAnnotations.append(annotation.trim());
+				classAnnotations.append("\n");
+                        }
+                }
+
+		return classAnnotations.toString();
+	}
 	
 	public String getClassModifiers() {
 		String classModifiers = null;
Index: tools/src/java/org/hibernate/tool/hbm2x/pojo/POJOClass.java
===================================================================
--- tools/src/java/org/hibernate/tool/hbm2x/pojo/POJOClass.java	(revision 10123)
+++ tools/src/java/org/hibernate/tool/hbm2x/pojo/POJOClass.java	(working copy)
@@ -24,6 +24,8 @@
 	 * @return
 	 */
 	public String getPackageDeclaration();
+
+	public String getClassAnnotations();
 	
 	public String getClassModifiers();
 
Index: tools/src/templates/pojo/PojoTypeDeclaration.ftl
===================================================================
--- tools/src/templates/pojo/PojoTypeDeclaration.ftl	(revision 10123)
+++ tools/src/templates/pojo/PojoTypeDeclaration.ftl	(working copy)
@@ -2,4 +2,4 @@
 ${pojo.getClassJavaDoc(pojo.getDeclarationName() + " generated by hbm2java", 0)}
  */
 <#include "Ejb3TypeDeclaration.ftl"/>
-${pojo.getClassModifiers()} ${pojo.getDeclarationType()} ${pojo.getDeclarationName()} ${pojo.getExtendsDeclaration()} ${pojo.getImplementsDeclaration()}
\ No newline at end of file
+${pojo.getClassAnnotations()}${pojo.getClassModifiers()} ${pojo.getDeclarationType()} ${pojo.getDeclarationName()} ${pojo.getExtendsDeclaration()} ${pojo.getImplementsDeclaration()}
Index: build.sh
===================================================================
--- build.sh	(revision 10123)
+++ build.sh	(working copy)
@@ -1,4 +1,4 @@
 #!/bin/sh
 CURDIR=`dirname $0`
-HIBERNATECORE=$CURDIR/../hibernate-3.1
+HIBERNATECORE=$CURDIR/../hibernate-3.2
 java -cp "$HIBERNATECORE/lib/ant-launcher-1.6.5.jar" org.apache.tools.ant.launch.Launcher -lib $HIBERNATECORE/lib $*
