--- hibernate-3.1beta1/src/org/hibernate/criterion/SimpleExpression.java	Thu Jul 21 13:42:02 2005
+++ hibernate-3.1beta1-patches/src/org/hibernate/criterion/SimpleExpression.java	Fri Jul 22 11:58:57 2005
@@ -48,7 +48,8 @@
 		if (columns.length>1) fragment.append('(');
 		for ( int i=0; i<columns.length; i++ ) {
 			SessionFactoryImplementor factory = criteriaQuery.getFactory();
-			boolean lower = ignoreCase && type.sqlTypes( factory )[i]==Types.VARCHAR;
+			int[] sqlTypes = type.sqlTypes(factory);
+			boolean lower = ignoreCase && (sqlTypes[i]==Types.VARCHAR || sqlTypes[i]==Types.CHAR);
 			if (lower) {
 				fragment.append( factory.getDialect().getLowercaseFunction() )
 					.append('(');
