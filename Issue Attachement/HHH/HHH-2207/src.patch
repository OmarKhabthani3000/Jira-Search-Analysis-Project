Index: org/hibernate/loader/Loader.java
===================================================================
--- org/hibernate/loader/Loader.java	(revision 10727)
+++ org/hibernate/loader/Loader.java	(working copy)
@@ -1631,9 +1631,10 @@
 			QueryParameters queryParameters,
 			int col,
 			SessionImplementor session) throws SQLException {
-		col += bindPositionalParameters( st, queryParameters, col, session );
-		col += bindNamedParameters( st, queryParameters.getNamedParameters(), col, session );
-		return col;
+        int tempCol = col;
+		tempCol += bindPositionalParameters( st, queryParameters, tempCol, session );
+		tempCol += bindNamedParameters( st, queryParameters.getNamedParameters(), tempCol, session );
+		return tempCol - col;
 	}
 
 	/**
Index: org/hibernate/loader/hql/QueryLoader.java
===================================================================
--- org/hibernate/loader/hql/QueryLoader.java	(revision 10727)
+++ org/hibernate/loader/hql/QueryLoader.java	(working copy)
@@ -501,7 +501,7 @@
 			ParameterSpecification spec = ( ParameterSpecification ) itr.next();
 			position += spec.bind( st, queryParameters, session, position );
 		}
-		return position;
+		return position - startPosition;
 	}
 
 	private int bindFilterParameterValues(
