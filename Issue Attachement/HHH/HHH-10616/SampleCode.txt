 @Test
    public void testMultipleResults() throws ParseException {
        List<List<Object>> returnList=new ArrayList<>();
        CrudService crudService=tenantCrudService();
        EntityManager em=crudService.getEntityManager();
        StoredProcedureQuery spq = em.createStoredProcedureQuery("[dbo].[testProc]");
        boolean isResultSet = spq.execute();
        List results2 = spq.getResultList();
        returnList.add(results2);
        if (spq.hasMoreResults()){
            returnList.add(spq.getResultList());
            System.out.print("Results");
        }
        System.out.println("ends here");

    }