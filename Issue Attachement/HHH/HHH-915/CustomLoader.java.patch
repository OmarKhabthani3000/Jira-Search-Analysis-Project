20d19
< import org.hibernate.loader.CollectionAliases;
22a22
> import org.hibernate.loader.CollectionAliases;
41,50d40
< 	
< 	/** 
< 	 * hc: Returns true if the query actually returns no result; false otherwise.
< 	 * A query with no result is indicated by specifying a scaler alias of "null".
< 	 * An example is a native SQL for bulk insertion.  
< 	 */
< 	public boolean hasNoResult() {
< 		String[] a = customQuery.getScalarColumnAliases();
< 		return a.length == 1 && "null".equals(a[0]);
< 	}
143,144c133
< 				String alias = scalarColumnAliases[i];
< 				resultRow[i] ="null".equals(alias) ? null : scalarTypes[i].nullSafeGet(rs, alias, session, null);
---
> 				resultRow[i] = scalarTypes[i].nullSafeGet(rs, scalarColumnAliases[i], session, null);
