Index: org/hibernate/loader/criteria/CriteriaLoader.java
===================================================================
--- org/hibernate/loader/criteria/CriteriaLoader.java	(revision 20078)
+++ org/hibernate/loader/criteria/CriteriaLoader.java	(working copy)
@@ -140,7 +140,7 @@
 			String[] columnAliases = translator.getProjectedColumnAliases();
 			for ( int i=0, pos=0; i<result.length; i++ ) {
 				int numColumns = types[i].getColumnSpan( session.getFactory() );
-				if ( numColumns > 1 ) {
+				if ( numColumns != 1 ) { // if 0 use an empty array for columnAliases
 			    	String[] typeColumnAliases = ArrayHelper.slice( columnAliases, pos, numColumns );
 					result[i] = types[i].nullSafeGet(rs, typeColumnAliases, session, null);
 				}

