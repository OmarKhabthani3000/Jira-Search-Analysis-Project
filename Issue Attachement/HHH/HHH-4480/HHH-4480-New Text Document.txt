
http://sourceforge.net/projects/hibernatesynch/ 
 



1.By using createSQLQuery

                                      

String qry ="select * from mas_employee"; 

objectList = (List) session.createSQLQuery(qry).list();

Vector vector=new Vector(objectList);

2.Using Criteria(only select queries)

List<MasEmployee> emList=new Array<MasEmployee>();

emList =session.createCriteria(MasStoreItem..class).list();

Vector vector=new Vector(emList);

3.By using HQL :

List<MasEmployee> emList=new Array<MasEmployee>();

emList =session.find(“from MasEmployee”);

Vector vector=new Vector(emList);

